using System;

namespace nilnul.num.stream
{
	/// <summary>
	/// 
	/// </summary>
	///  alias:
	///		series
	///		summation
	///		summary
	///		sum
	///			used for str.Sum()

	[Obsolete(nameof(ISeries))]
	public interface ISummation:subsume_.IAdd { }
}
